# RDP session crash (mstsc.exe)

## Context
Работа велась внутри корпоративной сети (доменная инфраструктура, доступ к системам через RDP, включая 1С).

Пользователи жаловались, что не могут нормально работать в 1С через RDP. При открытии сессии - закрывалась сама по себе.

## Problem
При подключении:
- RDP открывается
- но через короткое время падает
- любые действия в 1С выполнить невозможно

Сначала подумал, что проблема в самой 1С.

## Investigation
Начал с проверки логов Windows (Event Viewer).

Нашёл:
- Application Error (Event ID 1000)
<img width="752" height="447" alt="photo_2026-04-30_00-17-35" src="https://github.com/user-attachments/assets/106bf016-c121-4798-9bea-da46af3e53a1" />
  
- падает процесс `mstsc.exe`

- код ошибки: `0xc0000005`
<img width="616" height="429" alt="иууиуииу" src="https://github.com/user-attachments/assets/883b0a9f-732c-468e-ba48-63f05c6810fb" />

По коду ошибки стало понятно, что это проблема на уровне доступа к памяти.

В ошибке фигурировал файл:

- `LMFX1N4Z.DLL`
<img width="1160" height="150" alt="ИУЦ" src="https://github.com/user-attachments/assets/26f53b37-3477-4d63-8d11-7223878c9506" />

Проверил, где он находится:
- папка `spool\drivers` драйвер печати

## Analysis
Стало понятно, что проблема не в 1С, а в самом RDP-клиенте.

Связал это с тем, что:
- в RDP используется проброс локальных устройств
- включая принтеры

Предположил, что проблема может быть в драйвере печати при редиректе.

## Solution №1
Нужно файл подключения открыть с помощью текствого редактора и попать в его конфиг (.rdp).
- Был параметр: redirectprinters:i:1
- Поменял на: redirectprinters:i:0

Данный параметр отвечает за проброс локальных принтеров в RDP-сессию. С учётом выявленной причины сбоя, изменение выполнено внутри конфига путём ручного редактирования файла .rdp через текстовый редактор (Блокнот), поскольку в корпоративной среде изменение параметров через графический интерфейс может быть ограничено правами доступа. После внесения изменений подключение стабилизировалось, удалённая сессия не завершается аварийно.

## Solution №2
На втором устройстве при попытке применения способа №1 и редактирования конфигурации .rdp-файла возникала ошибка повреждения файла, что не позволило применить изменение стандартным способом.
<img width="536" height="395" alt="image" src="https://github.com/user-attachments/assets/50f1cbd3-778c-404e-a17f-d9b10c94f12a" />

В качестве обходного решения было выполнено:
- Был вернут параметр: redirectprinters:i: с "0" обратно на "1"
- Локальное дублирование библиотеки LMFX1N4Z.DLL в рабочий путь запуска (.rdp-файла).
  <img width="1171" height="555" alt="image" src="https://github.com/user-attachments/assets/ce0a1164-7815-45cc-9b3d-de9b4eefee76" />

Такой вариант сработал потому, что Windows ищет DLL не только в System32, а в определённом порядке:
- папка запуска приложения
- системные каталоги
- PATH и т.д.

Поэтому mstsc.exe начал использовать локальную копию библиотеки, а не проблемную версию из каталога spool\drivers\x64\3. Это позволило избежать загрузки проблемного модуля из системного каталога и стабилизировать работу RDP-сессии.

## Result
После применения решения:
- RDP-сессия перестала аварийно завершаться
- подключение стабилизировалось
  
## Notes
Root cause: конфликт драйвера печати, загружаемого в процесс mstsc.exe при пробросе локальных принтеров в RDP-сессию.
